{
  "name": "form-manager",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "tsc && ./post-build.sh",
    "push": "npx clasp push -f",
    "deploy": "npm run build && npm run push",
    "dev": "npm run build && npm run push",
    "watch": "nodemon --watch src --ext ts,html,json --exec 'npm run dev'",
    "login": "npx clasp login --no-localhost",
    "hash_password": "gpg -o .clasprc.json.gpg --symmetric --cipher-algo AES256 $HOME/.clasprc.json"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/google-apps-script": "^2.0.4",
    "clasp": "^1.0.0",
    "nodemon": "^3.1.10",
    "typescript": "^5.9.2"
  }
}
